apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app-chart.fullname" . }}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: task4-label-dep
  template:
    metadata:
      labels:
        app: task4-label-dep
    spec:
      containers:
        - name: {{ include "app-chart.fullname" . }}
          image: gorasu/task4:{{ .Values.app.version }}
          ports:
            - name: web
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: {{ include "app-chart.fullname" . }}
